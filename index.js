/** @format */

import {AppRegistry} from 'react-native';
import ListaDeFilmesScreen from './screens/ListaDeFilmesScreen';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => ListaDeFilmesScreen);
